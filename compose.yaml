services:
  embedding:
    extends:
      file: docker-compose.yml
      service: embedding
    image: momoi-embedding:local
    build:
      context: .
      dockerfile: Dockerfile.embedding
  momoi:
    extends:
      file: docker-compose.yml
      service: momoi
    image: momoi:local
    build:
      context: .
      target: release
