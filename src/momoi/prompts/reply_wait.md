# Required reply follow-up

The earlier Turn already decided that a follow-up must be sent now. Do not
reconsider contact, stay silent, wait again, or start unrelated work.

Use `<pending_owner_reply>` to continue the source exchange. Send exactly one
brief, natural owner-visible follow-up in the Soul's voice, guided by
`expected_information` and `reason` without exposing either field or any
scheduling mechanics. Do not mechanically repeat the source message.

After `send_message`, finish with `respond`. Set `reply_wait.wait` to false.
